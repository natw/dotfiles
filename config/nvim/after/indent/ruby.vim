" the bundled ruby indent.vim includes `.` in `indentkeys` for reasons
" I can't quite figure. This seems to not play nice with treesitter.
" And to be honest, I don't understand why you'd ever want it.
" set indentkeys-=.
